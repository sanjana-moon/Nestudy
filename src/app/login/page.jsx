import Login from "@/component/pages/Login";

export const metadata = {
    title: "Login | Nestudy",
    description: "...",
};

const LoginPage = () => {
   
    return (
        <Login/>
    );
};

export default LoginPage;